puppet-archmngt

This is the puppet-archmngt module.

If you want to extract from a zip archive you must include
https://github.com/akquinet/puppet-unzip.git and place an
"include unzip" or "class { "unzip" : }" in your recipe.  